<Window x:Class="DevToolVaultV2.Features.Start.StartWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="DevToolVaultV2"
        Height="700" Width="1000"
        WindowStartupLocation="CenterScreen"
        Background="#0F0F17"
        FontFamily="Segoe UI Variable">

    <Window.Resources>
        <!-- Modern sidebar button style -->
        <Style x:Key="SidebarButtonStyle" TargetType="Button">
            <Setter Property="Foreground" Value="#B8BCC8"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="16,12"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border CornerRadius="10" x:Name="ButtonBorder" Background="Transparent" Margin="8,2">
                            <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center" Margin="12,0"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#1A1A2E"/>
                                <Setter Property="Foreground" Value="#FFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <DockPanel>
        <!-- Modern sidebar with improved styling and spacing -->
        <Border DockPanel.Dock="Left" Width="220" Background="#16213E" CornerRadius="0,20,20,0">
            <StackPanel Margin="0,32,0,32">
                <TextBlock Text="DevToolVaultV2" FontSize="24" FontWeight="Bold" Foreground="#FFFFFF" Margin="24,0,24,40" TextAlignment="Center"/>
                
                <StackPanel Margin="8,0">
                    <Button Content="📊 Estrutura" Command="{Binding VisualizarEstruturaCommand}" Style="{StaticResource SidebarButtonStyle}" Margin="0,4"/>
                    <Button Content="📤 Exportar Código" Command="{Binding ExportarCodigoCommand}" Style="{StaticResource SidebarButtonStyle}" Margin="0,4"/>
                    <Button Content="🌳 Tree to Files" Command="{Binding TreetoFilesCommand}" Style="{StaticResource SidebarButtonStyle}" Margin="0,4"/>
                    <Button Content="🔧 Gerenciar Filtros" Command="{Binding GerenciarFiltrosCommand}" Style="{StaticResource SidebarButtonStyle}" Margin="0,4"/>
                    <Button Content="🔄 Recarregar Filtros" Command="{Binding RecarregarFiltrosCommand}" Style="{StaticResource SidebarButtonStyle}" Margin="0,4"/>
                </StackPanel>
                
                <Border Height="1" Background="#2A3F5F" Margin="24,32"/>
                
                <StackPanel Margin="8,0">
                    <Button Content="ℹ️ Sobre" Command="{Binding SobreCommand}" Style="{StaticResource SidebarButtonStyle}" Margin="0,4"/>
                    <Button Content="🚪 Sair" Command="{Binding SairCommand}" Style="{StaticResource SidebarButtonStyle}" Margin="0,4"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- Enhanced main content area with better typography and spacing -->
        <ScrollViewer VerticalScrollBarVisibility="Auto" Background="#0F0F17">
            <StackPanel Margin="40,32">
                <StackPanel Margin="0,0,0,40">
                    <TextBlock Text="Bem-vindo ao DevToolVaultV2" FontSize="36" FontWeight="Light" Foreground="#FFFFFF" Margin="0,0,0,8"/>
                    <TextBlock Text="Sua coleção de ferramentas de desenvolvimento" FontSize="16" Foreground="#B8BCC8" FontWeight="Normal"/>
                </StackPanel>
                
                <ItemsControl ItemsSource="{Binding Cards}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel Orientation="Horizontal" HorizontalAlignment="Center"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Button Command="{Binding OpenCommand}" 
                                    Background="Transparent" 
                                    BorderThickness="0" 
                                    Padding="0" 
                                    Margin="16"
                                    Width="280" Height="240"
                                    Cursor="Hand"
                                    RenderTransformOrigin="0.5,0.5">
                                <Button.RenderTransform>
                                    <ScaleTransform ScaleX="1" ScaleY="1"/>
                                </Button.RenderTransform>
                                
                                <Button.Template>
                                    <ControlTemplate TargetType="Button">
                                        <Border x:Name="CardBorder"
                                                CornerRadius="20"
                                                Padding="24"
                                                Width="280" Height="240"
                                                BorderThickness="1">
                                            <Border.Background>
                                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1" Opacity="0.1">
                                                    <GradientStop Color="#FFFFFF" Offset="0"/>
                                                    <GradientStop Color="#E0E0E0" Offset="1"/>
                                                </LinearGradientBrush>
                                            </Border.Background>
                                            <Border.BorderBrush>
                                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1" Opacity="0.2">
                                                    <GradientStop Color="#FFFFFF" Offset="0"/>
                                                    <GradientStop Color="#CCCCCC" Offset="1"/>
                                                </LinearGradientBrush>
                                            </Border.BorderBrush>
                                            <Border.Effect>
                                                <DropShadowEffect Color="#000000" BlurRadius="20" ShadowDepth="0" Opacity="0.15"/>
                                            </Border.Effect>
                                            
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                        
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Trigger.EnterActions>
                                                    <BeginStoryboard>
                                                        <Storyboard>
                                                            <DoubleAnimation Storyboard.TargetProperty="RenderTransform.(ScaleTransform.ScaleX)" To="1.05" Duration="0:0:0.3">
                                                                <DoubleAnimation.EasingFunction>
                                                                    <CubicEase EasingMode="EaseOut"/>
                                                                </DoubleAnimation.EasingFunction>
                                                            </DoubleAnimation>
                                                            <DoubleAnimation Storyboard.TargetProperty="RenderTransform.(ScaleTransform.ScaleY)" To="1.05" Duration="0:0:0.3">
                                                                <DoubleAnimation.EasingFunction>
                                                                    <CubicEase EasingMode="EaseOut"/>
                                                                </DoubleAnimation.EasingFunction>
                                                            </DoubleAnimation>
                                                            <DoubleAnimation Storyboard.TargetName="CardBorder" Storyboard.TargetProperty="Effect.(DropShadowEffect.BlurRadius)" To="30" Duration="0:0:0.3"/>
                                                            <DoubleAnimation Storyboard.TargetName="CardBorder" Storyboard.TargetProperty="Effect.(DropShadowEffect.Opacity)" To="0.25" Duration="0:0:0.3"/>
                                                        </Storyboard>
                                                    </BeginStoryboard>
                                                </Trigger.EnterActions>
                                                <Trigger.ExitActions>
                                                    <BeginStoryboard>
                                                        <Storyboard>
                                                            <DoubleAnimation Storyboard.TargetProperty="RenderTransform.(ScaleTransform.ScaleX)" To="1" Duration="0:0:0.3">
                                                                <DoubleAnimation.EasingFunction>
                                                                    <CubicEase EasingMode="EaseOut"/>
                                                                </DoubleAnimation.EasingFunction>
                                                            </DoubleAnimation>
                                                            <DoubleAnimation Storyboard.TargetProperty="RenderTransform.(ScaleTransform.ScaleY)" To="1" Duration="0:0:0.3">
                                                                <DoubleAnimation.EasingFunction>
                                                                    <CubicEase EasingMode="EaseOut"/>
                                                                </DoubleAnimation.EasingFunction>
                                                            </DoubleAnimation>
                                                            <DoubleAnimation Storyboard.TargetName="CardBorder" Storyboard.TargetProperty="Effect.(DropShadowEffect.BlurRadius)" To="20" Duration="0:0:0.3"/>
                                                            <DoubleAnimation Storyboard.TargetName="CardBorder" Storyboard.TargetProperty="Effect.(DropShadowEffect.Opacity)" To="0.15" Duration="0:0:0.3"/>
                                                        </Storyboard>
                                                    </BeginStoryboard>
                                                </Trigger.ExitActions>
                                            </Trigger>
                                            <Trigger Property="IsPressed" Value="True">
                                                <Setter Property="RenderTransform">
                                                    <Setter.Value>
                                                        <ScaleTransform ScaleX="0.98" ScaleY="0.98"/>
                                                    </Setter.Value>
                                                </Setter>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Button.Template>
                                
                                <StackPanel VerticalAlignment="Center">
                                    <TextBlock Text="{Binding Icon}" FontSize="52" HorizontalAlignment="Center" Margin="0,0,0,16" Opacity="0.9"/>
                                    <TextBlock Text="{Binding Title}" FontSize="20" FontWeight="SemiBold" Foreground="#FFFFFF" HorizontalAlignment="Center" Margin="0,0,0,8" TextAlignment="Center"/>
                                    <TextBlock Text="{Binding Description}" FontSize="14" Foreground="#B8BCC8" TextWrapping="Wrap" TextAlignment="Center" LineHeight="20" Margin="0,0,0,20"/>
                                    
                                    <!-- Visual indicator that card is clickable -->
                                    <Border Background="#1A1A2E" CornerRadius="8" Padding="12,6" HorizontalAlignment="Center" Opacity="0.7">
                                        <TextBlock Text="Clique para abrir" FontSize="12" Foreground="#B8BCC8" FontStyle="Italic"/>
                                    </Border>
                                </StackPanel>
                            </Button>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </ScrollViewer>
    </DockPanel>
</Window>