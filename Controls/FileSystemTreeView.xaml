<!-- DevToolVaultV2/Controls/FileSystemTreeView.xaml -->
<UserControl x:Class="DevToolVaultV2.Controls.FileSystemTreeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:DevToolVaultV2.Converters"
             xmlns:models="clr-namespace:DevToolVaultV2.Core.Models"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.Resources>
        <local:IconConverter x:Key="IconConverter" />
    </UserControl.Resources>

    <Grid>
        <TreeView x:Name="treeView">
            <TreeView.ItemContainerStyle>
                <Style TargetType="TreeViewItem">
                    <Setter Property="IsExpanded" Value="{Binding IsExpanded, Mode=TwoWay}" />
                </Style>
            </TreeView.ItemContainerStyle>
            <TreeView.ItemTemplate>
                <HierarchicalDataTemplate ItemsSource="{Binding Children}" DataType="{x:Type models:FileSystemItem}">
                    <StackPanel Orientation="Horizontal">
                        <CheckBox IsChecked="{Binding IsChecked, Mode=TwoWay}"
                                  IsThreeState="True"
                                  VerticalAlignment="Center"
                                  Margin="0,0,8,0"
                                  ToolTip="Selecionar/Deselecionar este item para exportação" />
                        <!-- Icon based on file type -->
                        <TextBlock Text="{Binding IsDirectory, Converter={StaticResource IconConverter}}" 
                                   FontFamily="Segoe UI Emoji"
                                   FontSize="16"
                                   VerticalAlignment="Center"
                                   Margin="0,0,5,0" />
                        <TextBlock Text="{Binding Name}" 
                                   VerticalAlignment="Center"
                                   ToolTip="{Binding FullPath}" />
                    </StackPanel>
                </HierarchicalDataTemplate>
            </TreeView.ItemTemplate>
        </TreeView>
    </Grid>
</UserControl>